{
  "name": "verumlingua",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "validate": "node scripts/validate_runtime.mjs && node scripts/check_filler.cjs && node scripts/audit_teach_content.mjs && node scripts/audit_mc_quality.mjs && node scripts/audit_placement_questions.mjs && node scripts/audit_boring_funfacts.mjs && node scripts/audit_pos_gender.mjs && node scripts/audit_hint_quality.mjs",
    "validate:full": "npm run validate && node scripts/pp63_audit.cjs --all --include-batch && node scripts/validate_structural_runtime.mjs",
    "prepare": "husky"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.4",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "ts-fsrs": "^5.3.2"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "husky": "^9.1.7",
    "vite": "^6.0.0"
  }
}
